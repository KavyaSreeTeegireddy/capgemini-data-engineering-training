Phase1: I have learned how to convert basic SQL queries into PySpark Dataframe operations.SQL works on structured tables,while PySpark uses DataFrames for handling large-scale           data.I understood how common operations like SELECT,WHERE and GROUP BY are implemented using functions like select(),filter() and groupBy() in PySpark.
