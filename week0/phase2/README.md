Phase2: Worked with PySpark DataFrames to read CSV files and handle missing data using dropna().
        Performed aggregations like total, average, and count using sum(), avg(), and count().
        Applied sorting and filtering to identify top customers and frequent buyers.
